export enum AssetTypeIconType {
    LANDSCAPE = 'landscape',
    SQUARE = 'square',
    PORTRAIT = 'portrait',
}

export enum CategoryID {
    ALL_ASSETS = 'ALL_ASSETS',
    ALL_MOVIES = 'ALL_MOVIES',
    SELECTED_MOVIE = 'SELECTED_MOVIE',
    SELECTED_SERIES = 'SELECTED_SERIES',
}

export enum ContentType {
    MOVIE = 'movie',
    SERIES = 'series',
    CALENDAR = 'calendar',
    EPISODE = 'episode',
    CAST = 'cast',
    DIRECTOR = 'director',
    PREVIEW = 'preview',
    TRAILER = 'trailer',
    LIVE = 'live',
}

export enum TypesWrcTimeZone {
    String = 'string',
}

export enum Championship {
    ERC = 'ERC',
    WRC = 'WRC',
    RX = 'RX',
}

export enum PurchaseMode {
    FREE = 'free',
    PAID = 'paid',
    REGISTER = 'register',
    RENTAL = 'rental',
}

export enum AccountTag {
    FREE = 'free',
    PREMIUM = 'premium',
}

export enum CurrentEventStatus {
    UPCOMING = 'UPCOMING',
    LIVE = 'LIVE',
    COMPLETED = 'COMPLETED',
    ON_NEXT = 'ON NEXT',
}

export enum AiringType {
    Live = 'live',
}

export enum Type {
    Landscape = 'landscape',
    Square = 'square',
}

export enum Scheme {
    Championship = 'championship',
}

export enum ProgramType {
    Episode = 'episode',
}

export enum AssetMetadata {
    TITLE = 'title',
    SUBTITLE = 'subTitle',
    DESCRIPTION = 'description',
    CLOSED_CAPTION_AVAILABILITY = 'closedCaptionAvailability',
    QUALITY_INDICATOR = 'qualityIndicator',
    YEAR = 'year',
    PARENTAL_CONTROL = 'parentalControl',
    PARENTAL_RATING = 'parentalRating',
    MEDIA_RATING = 'mediaRating',
    GENRE = 'genre',
    AVAILABILITY = 'availability',
    DURATION = 'duration',
    ACTOR = 'actor',
    DIRECTOR = 'director',
    EPISODE_COUNT = 'episodeCount',
    STUDIO = 'studio',
    COUNTRIES = 'countries',
}
